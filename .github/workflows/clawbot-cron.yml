name: Clawbot Auto-Run

on:
  workflow_dispatch:
  schedule:
    - cron: '*/10 * * * *' # 每10分钟运行一次

permissions:
  contents: write

jobs:
  auto-advance:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Python 3.11
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Initialize Database
      run: |
        python -c "from app.database import init_db; init_db()"
        echo "✅ Database initialized successfully"

    - name: Start FastAPI Server
      run: |
        uvicorn app.main:app --host 127.0.0.1 --port 8000 &
        echo "⏳ Waiting for server to start..."
        sleep 5
        echo "✅ FastAPI server started"

    - name: API Health Check
      run: |
        echo "🔍 Checking API health..."
        response=$(curl -s http://127.0.0.1:8000/)
        if [[ $response == *"Welcome to AgentTaskHub API!"* ]]; then
          echo "✅ API health check passed"
          echo "Response: $response"
        else
          echo "❌ API health check failed"
          exit 1
        fi

    - name: Test Root Endpoint
      run: |
        echo "🧪 Testing root endpoint..."
        response=$(curl -s -w "\n%{http_code}" http://127.0.0.1:8000/)
        http_code=$(echo "$response" | tail -n1)
        if [[ $http_code == "200" ]]; then
          echo "✅ Root endpoint test passed (HTTP $http_code)"
        else
          echo "❌ Root endpoint test failed (HTTP $http_code)"
          exit 1
        fi

    - name: Check Database Tables
      run: |
        echo "🗄️  Checking database tables..."
        python -c "
        from app.database import SessionLocal, engine
        from sqlalchemy import inspect
        inspector = inspect(engine)
        tables = inspector.get_table_names()
        print(f'✅ Found {len(tables)} tables: {tables}')
        if 'agents' in tables and 'tasks' in tables:
            print('✅ Required tables exist')
        else:
            print('❌ Missing required tables')
            exit(1)
        "

    - name: Add Task for Clawdbot
      run: |
        echo "🤖 Adding task to queue for Clawdbot..."
        python -c "
import json
from datetime import datetime

task = {
    'timestamp': datetime.now().isoformat(),
    'source': 'GitHub Actions',
    'task': 'Continue autonomous development on AgentTaskHub',
    'instructions': '''
Review the current project status and continue with the next task:

1. Check AGENT_TASKS.md for pending tasks
2. Test API endpoints (POST /agents/, POST /tasks/, etc.)
3. Improve code coverage
4. Add new features if testing is complete

Use exec tool with python cat_file.py to read files when needed.
''',
    'priority': 'normal'
}

try:
    with open('task_queue.json', 'r') as f:
        queue = json.load(f)
except:
    queue = {'pending_tasks': []}

queue['pending_tasks'].append(task)
queue['last_updated'] = datetime.now().isoformat()
queue['status'] = 'tasks_pending'

with open('task_queue.json', 'w') as f:
    json.dump(queue, f, indent=2)

print('Task added to queue successfully!')
print('Clawdbot will pick this up on next check.')
"

    - name: Notify Clawdbot via Telegram
      run: |
        echo "📢 Sending notification to Clawdbot via Telegram..."
        python -c "
import requests
import json
from datetime import datetime

# Telegram Bot Configuration
TELEGRAM_BOT_TOKEN = '8565919106:AAGDoAI6pT3hj8UG_TQaZiDd1fd3JRYZTuM'

# Get recent updates to find chat ID
try:
    updates_url = f'https://api.telegram.org/bot{TELEGRAM_BOT_TOKEN}/getUpdates'
    response = requests.get(updates_url)
    data = response.json()

    if data.get('ok') and data.get('result'):
        # Get the most recent chat ID
        last_update = data['result'][-1]
        chat_id = last_update['message']['chat']['id']

        # Send notification message
        message = f'''🔔 **GitHub Actions 任务提醒**

⏰ 时间: {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")} UTC
📝 来源: GitHub Actions Cron Workflow

🚀 **立即执行以下步骤：**

1. 读取 IDENTITY.md
   exec: {{ \"command\": \"python C:/Users/yueyo/clawd/cat_file.py C:/Users/yueyo/clawd/IDENTITY.md\", \"workdir\": \"C:/Users/yueyo/clawd\" }}

2. 检查任务队列
   exec: {{ \"command\": \"python C:/Users/yueyo/clawd/cat_file.py C:/Users/yueyo/clawd/AgentTaskHub/task_queue.json\", \"workdir\": \"C:/Users/yueyo/clawd\" }}

3. 执行 AUTOPILOT_MODE.md 中的指令
   - 自主工作
   - 遇到错误自动恢复
   - 持续推进项目

⚠️ **不要等待用户指令！立即开始工作！**

*Let's build something amazing! 🦞*'''

        send_url = f'https://api.telegram.org/bot{TELEGRAM_BOT_TOKEN}/sendMessage'
        payload = {
            'chat_id': chat_id,
            'text': message,
            'parse_mode': 'Markdown'
        }

        send_response = requests.post(send_url, json=payload)
        result = send_response.json()

        if result.get('ok'):
            print('✅ Notification sent to Clawdbot successfully!')
            print(f'📱 Chat ID: {chat_id}')
        else:
            print(f'❌ Failed to send notification: {result}')
    else:
        print('❌ No Telegram updates found. Please send a message to the bot first.')

except Exception as e:
    print(f'❌ Error: {e}')
"


    - name: Commit and Push Changes
      run: |
        git config user.name "github-actions[bot]"
        git config user.email "github-actions[bot]@users.noreply.github.com"
        git add AgentTaskHub/progress.md
        git commit -m "chore: auto-update progress.md from Clawbot cron workflow" || echo "No changes to commit"
        git push
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
